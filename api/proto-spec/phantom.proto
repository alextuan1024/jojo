syntax = "proto3";

package jojo;

service Phantom {
    rpc RejectHumanity (RejectHumanityReq) returns (RejectHumanityResp);
}

message RejectHumanityReq {
    string name = 1;
    int32 trace = 2;
}

message RejectHumanityResp {
    string line = 1;
    int32 trace = 2;
}